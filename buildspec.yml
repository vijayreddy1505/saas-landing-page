version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18  # Specify your Node.js version
    commands:
      - echo "Installing dependencies..."
      - npm install

  build:
    commands:
      - echo "Building the React app..."
      - npm run build

artifacts:
  files:
    - '**/*'

